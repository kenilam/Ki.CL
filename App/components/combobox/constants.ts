export const CLASS_NAME = 'kicl--components--combobox';

/** What the arrow keys move between inside the panel. */
export const FOCUSABLE =
  'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])';
