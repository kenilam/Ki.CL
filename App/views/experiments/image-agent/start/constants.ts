import { CLASS_NAME as VIEW } from '@/views/experiments/image-agent/constants';

/** Root of every class the start page owns. */
const CLASS_NAME = `${VIEW}__start`;

/** The article's anchor, for the link under How it works. */
const ARTICLE_ID = 'how-it-was-made';

export { ARTICLE_ID, CLASS_NAME };
