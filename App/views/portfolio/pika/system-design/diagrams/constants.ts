// Constants
import { CLASS_NAME } from '@/views/portfolio/pika/system-design/constants';

const BASE = `${CLASS_NAME}__diagram`;

/** Height of the elliptical cap on cylinder nodes. */
const CYLINDER_EDGE = 8;

export { BASE, CYLINDER_EDGE };
