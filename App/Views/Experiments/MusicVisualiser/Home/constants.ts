import { CLASS_NAME as VIEW } from '@/Views/Experiments/MusicVisualiser/constants';

/** Root of every class this view's index owns. */
const CLASS_NAME = `${VIEW}__home`;

/** The article's anchor, for the banner's chevron. */
const ARTICLE_ID = 'how-it-was-made';

export { ARTICLE_ID, CLASS_NAME };
